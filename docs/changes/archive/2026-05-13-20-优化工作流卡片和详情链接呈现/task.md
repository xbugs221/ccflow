## 1. 现状确认

- [x] 1.1 确认 `WorkflowStageProgress` 在项目概览和侧边栏的复用入口。
- [x] 1.2 确认 `WorkflowDetailView` 角色摘要行当前渲染 `sessionRef.label` 和重复 `✓` 的位置。
- [x] 1.3 确认 `workflow.artifacts` 是否足够推导当前轮次产物；不足时设计 `currentArtifactRef`。

## 2. 卡片阶段聚合

- [x] 2.1 在 `WorkflowStageProgress` 中构造聚合展示模型。
- [x] 2.2 将 `review_N` 聚合为单个审核图标并显示 `xN`。
- [x] 2.3 将 `repair_N` / `fix_N` 聚合为单个修复图标并显示 `xN`。
- [x] 2.4 保持规划、执行、归档等单阶段原有图标和状态颜色。
- [x] 2.5 聚合颜色按 active/failed 优先级计算，避免正在运行阶段被完成阶段覆盖。

## 3. 详情页角色行

- [x] 3.1 将角色行 session 按钮可见文本改为 `会话`，点击逻辑保持不变。
- [x] 3.2 将重复 `✓` 改为数字计数，例如 `x3`。
- [x] 3.3 为角色行选择当前轮次产物，并放在 `会话` 链接后。
- [x] 3.4 确保多轮审核只显示最大轮次产物，例如只显示 `review-2.json`。
- [x] 3.5 当前产物不存在时不渲染可点击的坏链接。

## 4. 测试

- [x] 4.1 在本提案 `tests/` 目录新增工作流卡片聚合业务测试。
- [x] 4.2 在本提案 `tests/` 目录新增详情页角色摘要业务测试。
- [x] 4.3 如新增 read model 字段，补充 server 测试覆盖当前轮次产物选择。
- [x] 4.4 更新或补充根目录相关测试，确保执行阶段可随常规测试套件运行。

## 5. 验证

- [x] 5.1 运行 `oz validate 20-优化工作流卡片和详情链接呈现 --json`。
- [x] 5.2 运行新增的工作流前端业务测试。
- [x] 5.3 运行受影响的 server read-model 测试。
- [x] 5.4 确认没有启动 sealed run，也没有创建 wo 运行态文件。
