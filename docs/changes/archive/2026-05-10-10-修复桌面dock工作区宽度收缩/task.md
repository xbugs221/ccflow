## 1. 复现和定位

- [x] 1.1 在桌面 1920px 视口打开真实 fixture 项目会话，复现右侧大空白和中心区域被压缩。
- [x] 1.2 记录主内容区域、dock 根节点、右侧 dock、聊天中心区域的 `boundingBox`。
- [x] 1.3 沿 `AppContent -> MainContent -> WorkspaceDockLayout` 检查 `flex`、`w-full`、`min-w-0`、`overflow` 约束。
- [x] 1.4 确认问题发生在 dock 根节点还是调用方父级容器，选择最小修复位置。

## 2. 修复桌面 dock 横向填充

- [x] 2.1 修正 dock 工作区根容器，使其在桌面主内容 body 内横向填满可用宽度。
- [x] 2.2 保持中心区域继续使用剩余宽度，右侧 dock 继续使用持久化固定宽度。
- [x] 2.3 确认项目会话、工作流会话、项目主页三个调用分支都获得相同布局。
- [x] 2.4 确认移动端 overlay 分支不受影响。

## 3. 保持 resize 行为

- [x] 3.1 验证向左拖动右侧 dock 左边界时，dock 变宽且右边界稳定。
- [x] 3.2 验证向右拖动右侧 dock 左边界时，dock 变窄且右边界稳定。
- [x] 3.3 验证底部 terminal dock 高度调整不受横向修复影响。
- [x] 3.4 验证右侧 dock 全屏、退出全屏、折叠和展开行为不受影响。

## 4. 真实测试代码

- [x] 4.1 在本提案 `tests/` 目录编写真实 Playwright 测试，执行阶段同步到仓库根测试套件。
- [x] 4.2 测试桌面项目会话中右侧 dock 右边界贴住主内容右边界。
- [x] 4.3 测试桌面项目会话中聊天中心区域占用剩余宽度且可用。
- [x] 4.4 测试右侧 dock resize 后右边界保持稳定。
- [x] 4.5 测试项目主页分支不会出现右侧大空白。

## 5. 验证

- [x] 5.1 运行 `oz validate 10-修复桌面dock工作区宽度收缩 --json`。
- [x] 5.2 运行新增 dock 宽度回归 Playwright 测试。
- [x] 5.3 运行既有 workspace dock layout 相关测试。
- [x] 5.4 检查本变更没有创建 `.wo/runs/` 运行态文件。
