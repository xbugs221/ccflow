## 1. 复现和定位

- [x] 1.1 使用 Playwright 或浏览器复现主会话区域无法上下滚动。
- [x] 1.2 使用 Playwright 或浏览器复现项目主页无法上下滚动。
- [x] 1.3 沿工作区父级容器检查 `height`、`min-height`、`overflow` 和 flex/grid 收缩约束，定位滚动被锁住的位置。
- [x] 1.4 记录当前顶部 tab、终端 pane、右侧 pane 的控制按钮 DOM 结构和 `data-testid`，避免实现时破坏测试定位。

## 2. 修复滚动

- [x] 2.1 修复聊天工作区父级高度链路，使消息列表拥有明确的纵向滚动容器。
- [x] 2.2 确认聊天 composer 在消息滚动时保持可见且可输入。
- [x] 2.3 修复项目主页中心区域滚动，避免内容被 dock 布局截断。
- [x] 2.4 确认右侧 pane 和底部终端展开时，聊天和项目主页仍可滚动。

## 3. 调整顶部 tab

- [x] 3.1 将 `消息`、`终端`、`文件`、`源代码管理` 顶部 tab 改为只显示 icon。
- [x] 3.2 保留每个 tab 的 `aria-label`、tooltip 或等价可访问名称。
- [x] 3.3 保留现有 `data-testid` 和选中态表达，避免破坏既有测试。
- [x] 3.4 确认 icon-only tab 在窄屏和桌面宽度下不出现文字或布局跳动。

## 4. 调整 pane 控制按钮

- [x] 4.1 将终端 pane 的移动和全屏按钮从底部移动到顶部控制条。
- [x] 4.2 将文件和源代码管理 pane 的移动和全屏按钮统一放在顶部控制条。
- [x] 4.3 移除终端、文件、源代码管理 pane 内部的折叠按钮。
- [x] 4.4 确认点击顶部对应 tab 可以完成 pane 折叠和展开。
- [x] 4.5 确认终端全屏、退出全屏、移动到右侧 split、移回底部等既有能力不被破坏。

## 5. 真实测试代码

- [x] 5.1 在本提案 `tests/` 目录编写真实 Playwright 测试，执行阶段同步到仓库根测试套件。
- [x] 5.2 测试长会话消息区域可上下滚动，且 composer 保持可用。
- [x] 5.3 测试项目主页内容溢出后可上下滚动。
- [x] 5.4 测试顶部 tab 只显示 icon，同时保留可访问名称和选中态。
- [x] 5.5 测试终端、文件、源代码管理 pane 的移动和全屏按钮位于顶部。
- [x] 5.6 测试 pane 内不再出现折叠按钮，折叠和展开通过顶部 tab 完成。
- [x] 5.7 更新已有 dock layout 测试中依赖 pane 折叠按钮的断言。

## 6. 验证

- [x] 6.1 运行 `oz validate 9-修复工作区滚动和dock面板控制 --json`。
- [x] 6.2 运行工作区 dock 布局相关 Playwright 测试。
- [x] 6.3 运行项目主页和会话工作区相关测试。
- [x] 6.4 检查本变更没有创建 `.wo/runs/` 运行态文件。
