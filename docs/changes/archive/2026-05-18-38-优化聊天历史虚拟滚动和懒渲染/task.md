# 任务：优化聊天历史虚拟滚动和懒渲染

## 1. 建立性能回归样例

- [x] 1.1 构造包含 1000 条以上混合消息的长会话 fixture，覆盖文本、Markdown、代码块、工具卡、diff 和子任务消息。
- [x] 1.2 增加浏览器测试，记录初始打开时不会全量请求历史。
- [x] 1.3 增加浏览器测试，断言初始打开和多次上滑后 `.chat-message` DOM 数量保持有界。
- [x] 1.4 确认现有历史滚动、搜索跳转、外部追加消息测试的失败点。

## 2. 引入虚拟滚动消息面板

- [x] 2.1 评估并接入成熟虚拟列表方案，或实现仅服务聊天 transcript 的最小虚拟窗口。
- [x] 2.2 将 `ChatMessagesPane` 从全量 `visibleMessages.map` 改为虚拟范围渲染。
- [x] 2.3 支持动态高度测量和稳定 message key。
- [x] 2.4 保持初始打开定位最新消息。
- [x] 2.5 保持顶部触发分页加载旧消息。
- [x] 2.6 加载旧消息后按锚点恢复滚动位置。
- [x] 2.7 新消息追加时保留底部跟随和用户上滑冻结语义。

## 3. 拆分加载状态和渲染状态

- [x] 3.1 将“已加载消息数量”和“DOM 可见窗口”从 `visibleMessageCount` 语义中拆开。
- [x] 3.2 调整 `loadAllMessages`，使其不再把全部消息完整渲染到 DOM。
- [x] 3.3 搜索跳转使用 message key 定位虚拟列表 item。
- [x] 3.4 对未加载搜索目标保留逐页加载直到命中的流程。

## 4. 工具卡和重内容懒渲染

- [x] 4.1 调整 `CollapsibleSection`，折叠时不创建完整 children。
- [x] 4.2 工具卡默认渲染摘要，展开后再渲染完整输入、结果、diff 或 Markdown。
- [x] 4.3 子任务容器默认显示状态和计数，展开后再渲染完整 step timeline。
- [x] 4.4 大代码块默认摘要化，展开后再执行完整高亮渲染。
- [x] 4.5 大 diff 和大工具输出默认摘要化，展开后再完整渲染。

## 5. 降低长会话同步成本

- [x] 5.1 审查 `convertSessionMessages`、`mergePersistedAndOptimisticMessages` 和 `dedupeAdjacentChatMessages` 的全量重算路径。
- [x] 5.2 能安全增量化的路径改为增量处理。
- [x] 5.3 限制 `chat_messages_*` localStorage 写入，只保存必要的本地恢复信息，避免长会话每次变化都同步 stringify。

## 6. 测试和验证

- [x] 6.1 新增长会话虚拟 DOM 上限测试。
- [x] 6.2 更新历史滚动锚点保持测试。
- [x] 6.3 更新聊天历史搜索跳转测试。
- [x] 6.4 新增工具卡折叠懒渲染测试。
- [x] 6.5 运行相关 server/spec/e2e 测试。
- [x] 6.6 运行 `oz validate 38-优化聊天历史虚拟滚动和懒渲染 --json`。
