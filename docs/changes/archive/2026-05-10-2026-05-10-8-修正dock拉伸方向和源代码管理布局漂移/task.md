## 1. 复现和定位

- [x] 1.1 使用 Playwright 或浏览器手动复现右侧 dock 拖动方向反的问题。
- [x] 1.2 使用 Playwright 或浏览器手动复现底部 terminal dock 拖动方向反的问题。
- [x] 1.3 记录点击 `源代码管理` 前后的 right dock bounding box，判断是状态宽度变化还是 Git 内容撑开布局。

## 2. 修正 resize 方向

- [x] 2.1 修正右侧 dock resize 方向：向左拖动增加宽度，向右拖动减少宽度。
- [x] 2.2 修正底部 dock resize 方向：向上拖动增加高度，向下拖动减少高度。
- [x] 2.3 保持现有最小/最大宽高 clamp，不引入无界尺寸。
- [x] 2.4 确认刷新后仍能恢复用户调整后的右侧宽度和底部高度。

## 3. 修正源代码管理右栏推进

- [x] 3.1 确认 `文件` / `源代码管理` 切换只更新 `rightDock.activePanel`，不重置或递增 `rightDock.width`。
- [x] 3.2 如果 Git 内容撑开 dock，补充 `min-w-0`、`overflow-hidden` 或内部滚动约束。
- [x] 3.3 确认 Git header 中分支、同步按钮和刷新按钮在窄右栏中不会扩大 dock 外框。
- [x] 3.4 确认聊天主体在切换 Git 后仍保持可见和可交互。

## 4. 真实测试代码

- [x] 4.1 在本提案 `tests/` 目录编写真实 Playwright 回归测试，执行阶段同步到仓库根测试套件。
- [x] 4.2 测试右侧 dock 向左拖动变宽、向右拖动变窄。
- [x] 4.3 测试底部 terminal dock 向上拖动变高、向下拖动变矮。
- [x] 4.4 测试从 `文件` 切换到 `源代码管理` 后，右侧 dock 宽度和左边界保持稳定。
- [x] 4.5 测试多次切换 `文件` / `源代码管理` 后，右侧栏不持续向左推进，聊天主体仍可见。
- [x] 4.6 测试终端全屏入口仍然存在，并能退出回到底部 dock。

## 5. 验证

- [x] 5.1 运行 `oz validate 2026-05-10-8-修正dock拉伸方向和源代码管理布局漂移 --json`。
- [x] 5.2 运行工作区 dock 布局相关 Playwright 测试。
- [x] 5.3 运行已有 shell、file tree、git panel 相关测试，确认业务组件未被布局修复破坏。
- [x] 5.4 检查本变更没有创建 `.wo/runs/` 运行态文件。
