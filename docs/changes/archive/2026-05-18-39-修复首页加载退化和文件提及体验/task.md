# 任务：修复首页加载退化和文件提及体验

## 1. 建立回归样例

- [x] 1.1 构造 Provider 索引慢或失败的 server 测试夹具。
- [x] 1.2 构造超过 50 个 Provider-only 项目的测试夹具。
- [x] 1.3 构造未知 `messageCount` 的项目主页和侧边栏展示测试。
- [x] 1.4 构造 `@文件`模糊搜索和文件树选择的浏览器测试。

## 2. 修复首页项目发现热路径

- [x] 2.1 梳理 `getProjects()` 中必须阻塞首页和可以后台完成的步骤。
- [x] 2.2 给 Codex、Pi、OpenCode Provider 索引增加失败隔离和耗时边界。
- [x] 2.3 在索引慢或失败时返回手动项目或已有缓存项目。
- [x] 2.4 Provider-only 项目按最近活跃排序并限制为 50 个。
- [x] 2.5 避免首页请求期间批量写入 routeIndex/config 等非必要持久化。
- [x] 2.6 增加必要的分阶段耗时日志，便于定位后续慢点。

## 3. 修复未知消息数语义

- [x] 3.1 后端 Provider 概览对未知消息数使用 `null`、省略字段或显式 `messageCountKnown = false`。
- [x] 3.2 更新前端 session view model，区分真实 0 和未知消息数。
- [x] 3.3 项目主页会话卡片在未知消息数时完全隐藏消息数。
- [x] 3.4 侧边栏会话卡片在未知消息数时完全隐藏消息数。
- [x] 3.5 保留真实正数消息数展示。

## 4. 改造 @文件选择体验

- [x] 4.1 保留当前项目文件树结构，默认展示项目根目录。
- [x] 4.2 把 `@文件`选择器拆成搜索输入、搜索结果、文件树三块状态。
- [x] 4.3 搜索输入非空时展示模糊搜索结果，结果数量保持有界。
- [x] 4.4 搜索输入为空时展示可展开折叠的文件树。
- [x] 4.5 点击或键盘确认文件后复用现有插入逻辑写入输入框。
- [x] 4.6 大仓库下避免默认渲染整棵已展开树。

## 5. 测试和验证

- [x] 5.1 新增或更新 server 测试，覆盖首页降级和 Provider-only 50 项上限。
- [x] 5.2 新增或更新 browser/spec 测试，覆盖未知消息数不显示 0。
- [x] 5.3 新增或更新 browser/spec 测试，覆盖 `@文件`模糊搜索。
- [x] 5.4 新增或更新 e2e 测试，覆盖文件树导航选择文件。
- [x] 5.5 在本机 `localhost:4001` 验证首页可进入，且会话卡片不再显示误导性 0。
- [x] 5.6 运行相关测试和 `oz validate 39-修复首页加载退化和文件提及体验 --json`。

## 执行记录

- 2026-05-18：历史测试 `provider-fast-discovery` 原先断言 header-only 会话 `messageCount` 为 `0`，与本提案“未知不能显示成 0”的新语义冲突，已更新为 `null`。
- 2026-05-18：`localhost:4001` 旧进程验证接口可达；为确认本轮源码效果，另启 `127.0.0.1:4002` 验证 `/api/projects` 返回 20 个项目、Provider-only 项目未超过 50、516 个 header-only 会话 `messageCount` 为 `null`、0 个误导性 `messageCount: 0`。
