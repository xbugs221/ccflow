## 1. 现状复核

- [x] 1.1 复核桌面 dock 当前产品意图：默认不展开右侧文件 dock 和底部终端 dock。
- [x] 1.2 复核移动端会话页 DOM 高度链路，记录实际滚动所有者。
- [x] 1.3 复核会话点击到消息接口调用链，区分 `cN` route id、provider session id、`routeIndex` 和空响应。

## 2. 桌面 dock 测试预期更新

- [x] 2.1 更新过期 Playwright 测试，删除默认右 dock 和底部 dock 必须出现的断言。
- [x] 2.2 新增或改写断言：默认会话主区、聊天输入框可见，辅助 dock 默认不可见。
- [x] 2.3 覆盖点击文件、终端、Git 后 dock 可打开，且会话区和输入框仍可见。

## 3. 移动端滚动和输入框

- [x] 3.1 修正移动端主内容和聊天容器高度收缩边界，保证 transcript 能成为滚动所有者。
- [x] 3.2 修正 composer 底部布局和 safe area 处理，保证输入框不被裁切或推到视口外。
- [x] 3.3 确认移动端从文件、终端、Git 单视图切回会话后，滚动和输入框状态正常。

## 4. 会话空白问题

- [x] 4.1 修正消息加载错误处理，避免接口失败被静默当作空历史。
- [x] 4.2 修正 `cN` 路由绑定 provider 会话后的消息来源选择。
- [x] 4.3 为真实空历史、草稿会话、加载失败分别提供明确可见状态。
- [x] 4.4 确认有历史消息的会话在移动端和桌面端都能显示真实消息。

## 5. 真实测试代码

- [x] 5.1 在本提案 `tests/` 目录编写真实 Playwright 回归测试，执行阶段同步到根测试套件。
- [x] 5.2 覆盖桌面默认 dock 不展开但可主动打开。
- [x] 5.3 覆盖移动端长会话 transcript 可滚动，滚动后 `scrollTop` 变化。
- [x] 5.4 覆盖移动端 composer 在视口内且可编辑。
- [x] 5.5 覆盖打开有历史消息的会话会渲染消息文本。
- [x] 5.6 覆盖真实空历史或草稿会话显示明确空状态。
- [x] 5.7 覆盖消息接口失败显示错误状态，不显示纯空白主区。

## 6. 验证

- [x] 6.1 运行 `oz validate 2026-05-11-17-修复移动端会话视图和更新dock预期 --json`。
- [x] 6.2 运行新增或更新的 Playwright 测试。
- [x] 6.3 运行相关桌面 dock、移动端单视图、历史滚动测试，确认没有回退。
- [x] 6.4 手动用移动端视口打开长会话和空历史会话，确认滚动、输入框和状态展示符合预期。
