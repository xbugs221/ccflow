## 1. tab 和 dock 状态

- [x] 1.1 梳理 `useProjectsState`、`MainContent`、`MainContentTabSwitcher` 中 `activeTab` 与 dock 状态的交互。
- [x] 1.2 调整桌面端默认 tab 读取逻辑，旧的 `files`、`shell`、`git` 持久化值不得成为主内容默认值。
- [x] 1.3 调整桌面端文件、终端、Git 按钮点击行为，只更新 dock 状态并保持会话区可见。
- [x] 1.4 保留移动端单视图切换行为，避免回退移动端文件、终端、Git 使用体验。
- [x] 1.5 更新 tab 选中态，让桌面默认只选中会话 tab。

## 2. 自动刷新边界

- [x] 2.1 审计普通项目页和会话页中的 `window.location.reload()`、`setInterval` 和项目列表刷新路径。
- [x] 2.2 移除或收窄普通页面的自动刷新逻辑，确保没有无业务必要的周期性 `/api/projects` 请求。
- [x] 2.3 保留运行中 workflow 详情的必要状态刷新，并确认它不会影响普通会话页。
- [x] 2.4 确认路由解析和项目列表更新不会把当前会话误清空。

## 3. 底部终端栏

- [x] 3.1 让底部 dock 终端使用嵌入式展示，不渲染包含“断开连接/重启”的 ShellHeader。
- [x] 3.2 在终端 dock header 或紧邻区域增加 `新建` 操作。
- [x] 3.3 增加项目内终端实例状态，支持多个独立终端。
- [x] 3.4 增加 `删除` 操作，关闭当前终端实例并清理对应 shell 连接。
- [x] 3.5 处理删除最后一个终端后的空态或默认终端恢复逻辑。
- [x] 3.6 确认新建、切换、删除终端不会影响中间会话区。

## 4. 文件 dock 去重

- [x] 4.1 调整 `FileTreeHeader`，在 dock 场景下不显示重复的“文件”标题。
- [x] 4.2 确认文件搜索、刷新、新建、上传、折叠和视图切换按钮仍可用。
- [x] 4.3 如移动端需要标题，增加上下文参数而不是恢复桌面重复标题。

## 5. 真实测试代码

- [x] 5.1 在本提案 `tests/` 目录编写真实 Playwright 回归测试，执行阶段同步到仓库根测试套件。
- [x] 5.2 覆盖默认打开真实会话路由时只有会话 tab 选中。
- [x] 5.3 覆盖点击文件、终端、Git 后会话区仍可见。
- [x] 5.4 覆盖普通会话页等待数秒不发生整页 reload，也不每秒轮询 `/api/projects`。
- [x] 5.5 覆盖底部终端 dock 不显示“断开连接/断开链接”和“重启”控制行。
- [x] 5.6 覆盖终端 `新建` 和 `删除` 行为。
- [x] 5.7 覆盖文件 dock 内部不重复显示“文件”标题，文件操作仍存在。

历史测试更新：`tests/2026-05-10-7-重新设计交互布局-workspace-dock-layout.test.js` 和 `tests/2026-05-10-12-区分移动端单视图和精简会话标题-12-mobile-single-view-workspace.test.js` 原先把文件、终端、Git dock 按钮断言为主选中 tab；这与本变更新意图冲突，已改为断言会话 tab 保持选中且 dock 正常打开、收起或切换。

## 6. 验证

- [x] 6.1 运行 `oz validate 2026-05-11-14-修复前端tab选中和终端细节 --json`。
- [x] 6.2 运行新增或更新的 Playwright 回归测试。
- [x] 6.3 运行相关已有 dock、移动端单视图、shell tab 测试，确认没有回退。
- [x] 6.4 手动确认普通会话页停留数秒不会自动刷新或清空。
